use debug::PrintTrait;
//the function print odd numbers should print all odd numbers from 1 to n
fn print_odd_numbers(n: i32) {
    let mut i = 1;
    let odd = 0;
    loop {
        if i>n {
            break;
        }
        if i % 2 != 0 {
             odd
        }
   return odd;
    }}
fn main() {
    let odn = print_odd_numbers(10);
    print.odn();
    
}
```